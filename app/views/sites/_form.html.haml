= simple_form_for @site, html: { class: 'form-horizontal col-md-6 col-md-offset-3 text-center', role: 'form' } do |f|


  = f.input :site_name

  = f.input :short_name

  = f.input :region

  = f.input :information
  .form-actions
    = link_to "Назад", Site,  class: 'btn btn-primary'
    = f.submit 'Сохранить', class: 'btn btn-primary'
